# 天气对比

把多个天气服务商对同一坐标的预报并排展示,让用户自己判断哪家更准。产品不替用户选,只负责把差异摆清楚。

## Language

### 数据可用性

同一个词在这个项目里有三种截然不同的含义,`CLAUDE.md` 里为此专门警告过"别搞混"。不要用"降级"这个笼统说法,改用下面三个:

**数据源失败**:
某个数据源整体不可用,`ProviderResult.status` 为 `'error'`,只带一句固定的中文用户文案。
_Avoid_: 降级、挂了

**字段缺失**:
数据源可用,但某些字段取不到,`status` 仍是 `'ok'`,缺的字段为 `null` 或 `[]`。**只要拿到任意一部分数据就算成功。**
_Avoid_: 降级、部分失败

**模拟回落**:
上游失败时返回编造的数据充当结果。本项目**不做这件事** —— 术语列在这里是为了在讨论中能一眼指认它,并与上面两者区分开。
_Avoid_: 降级、mock 兜底

### 分歧判定

三个词是三个不同的层级,不要混用:

**共识**:
对当前时刻各数据源整体一致程度的单一判定,三档:高度一致 / 大体吻合 / 存在明显分歧。整个页面只有一个共识结论。
_Avoid_: 一致性、总体一致度

**分歧**:
某个具体维度上各数据源不一致(实况温度极差、实况降水判定、某个时刻的降水预报)。分歧是共识判定的输入。
_Avoid_: 差异、不一致度

**一致性**:
逐日预报里**某一天**的评级,三档:high / moderate / low。每天一个,与整体的"共识"不是一回事。
_Avoid_: 共识、吻合度

**极差**:
同一指标在各数据源之间的最大值减最小值。分歧判定一律用极差而不是"两家之差",这样新增数据源时判定自动覆盖它。
_Avoid_: 差值、温差(指两家时的特例)

### 部署前缀

三个前缀各管一件事,名字相近但作用域完全不同:

**站点前缀**:
反向代理上这个应用挂载的路径(如 `/weather-app/`),由 nginx 的 `location` 决定。
_Avoid_: base、路径前缀

**接口前缀**:
后端 `setGlobalPrefix` 加在所有接口路由前的一段(环境变量 `API_PREFIX`,生产为 `api`)。
_Avoid_: base path

**资源前缀**:
前端构建产物里静态资源的引用前缀(环境变量 `VITE_BASE_PATH`),**构建时**写死,改了必须重新构建。
_Avoid_: base、public path
