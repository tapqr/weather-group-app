# 后端地址。本地开发指向后端 dev server;
# 子路径部署时用相对路径(如 /weather-app/api),同域请求不触发 CORS
VITE_API_BASE_URL=http://localhost:3000

# 静态资源引用前缀,**构建时**生效(改了必须重新 build)。
# 本地开发不用设;部署到子路径时设为 /weather-app/,详见 docs/DEPLOYMENT.md
# VITE_BASE_PATH=/weather-app/
